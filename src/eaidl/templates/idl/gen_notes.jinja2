{# Macro for generating notes for object, need notes field. #}
{% macro gen_notes(cls) -%}
{%- if cls.notes and cls.notes|trim -%}
/**
{{ cls.notes | trim | wordwrap(78) | indent(4, true) }}
*/{{ '\n' -}}
{%- endif -%}
{%- endmacro %}